# Инструкция по запуску test_line.py в Docker

## 📋 Описание
Этот Dockerfile создает контейнер для запуска скрипта обработки видео с использованием OpenCV.

## 🐳 Построение Docker образа

```bash
docker build -t test-line-app .
```

## ▶️ Запуск контейнера

### Вариант 1: С доступом к веб-камере (Linux/Mac)
```bash
docker run --rm \
  --device /dev/video0 \
  -e DISPLAY=$DISPLAY \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  test-line-app
```

### Вариант 2: С доступом к веб-камере (Windows с WSL2)
```bash
docker run --rm \
  --device /dev/video0 \
  -e DISPLAY=host.docker.internal:0 \
  test-line-app
```

### Вариант 3: Без графического интерфейса (сохранение кадров)
Если вам не нужен интерактивный просмотр, отредактируйте скрипт для сохранения кадров в файл.

## 📝 Примечания

⚠️ **Важно:** 
- Скрипт требует доступа к веб-камере (`/dev/video0`)
- Требуется X11 сервер (Linux/Mac) или WSL2 (Windows)
- На некоторых системах может не работать из-за ограничений доступа к камере

## 🔧 Решение проблем

**Ошибка "не удалось открыть камеру":**
- Убедитесь, что камера не используется другим приложением
- Проверьте доступ: `ls -l /dev/video*`

**Нет дисплея:**
- На Linux убедитесь, что установлены пакеты: `sudo apt-get install libgl1-mesa-glx libglib2.0-0`
- Запустите `xhost +local:docker` перед запуском контейнера

## 📊 Структура контейнера

- **Base Image:** python:3.11-slim
- **Рабочая директория:** /app
- **Точка входа:** python test_line.py
- **Зависимости:** opencv-python, numpy
