# ESP32 Robot Client 🤖

Код на C++ для ESP32 микроконтроллера, который управляет движением робота и коммуницирует с сервером через TCP.

## 📁 Структура

```
esp32/
├── src/
│   └── main.cpp              # Главный код ESP32
├── include/                  # Заголовочные файлы (при необходимости)
├── platformio.ini            # Конфигурация PlatformIO
├── .gitignore               # Git исключения (для .pio/)
└── README.md                # Этот файл
```

## 🔧 Требования

- **PlatformIO CLI** - для сборки и загрузки
  ```bash
  pip install platformio
  ```
- **ESP32 развивающая плата** (например, ESP32-DEVKIT-V1)
- **USB кабель** для подключения и загрузки

## ⚙️ Настройка

### 1. WiFi конфигурация

Отредактируйте переменные в `src/main.cpp`:

```cpp
static const char* WIFI_SSID = "YOUR_WIFI_SSID";        // Ваш WiFi SSID
static const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";    // Ваш пароль WiFi
static const char* SERVER_IP = "192.168.1.143";         // IP адрес сервера
static const uint16_t SERVER_PORT = 3333;               // Порт сервера
```

### 2. Установка драйверов (если нужны)

Убедитесь что установлены USB драйверы для ESP32.

## 🚀 Сборка и загрузка

### Сборка
```bash
cd esp32
pio run -e esp32dev
```

### Загрузка на плату
```bash
pio run -e esp32dev -t upload
```

### Мониторинг серийного порта
```bash
pio device monitor --baud 115200
```

### Все операции вместе
```bash
pio run -e esp32dev -t upload && pio device monitor
```

## 🔌 Подключение

После загрузки кода на ESP32:
1. Плата подключится к WiFi с указанными credentials
2. Плата подключится к TCP серверу на указанном IP:PORT
3. Сервер может отправлять команды для управления движением робота

## 📊 Отладка

### Просмотр логов
```bash
pio device monitor --baud 115200
```

### Общие проблемы

- **"Плата не найдена"** - проверьте USB подключение
- **"Не удается подключиться к WiFi"** - проверьте SSID и пароль в коде
- **"Не удается подключиться к серверу"** - проверьте IP адрес и порт

## 📚 Ссылки

- [PlatformIO документация](https://docs.platformio.org/)
- [ESP32 документация](https://docs.espressif.com/projects/esp-idf/en/latest/)
- [Arduino для ESP32](https://github.com/espressif/arduino-esp32)
