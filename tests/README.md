# Tests 🧪

Модульные и интеграционные тесты для проекта BattleBot.

## 📁 Структура

```
tests/
├── __init__.py              # Инициализация модуля
├── test_matrix.py           # Тесты для матриц и A* алгоритма
├── test_line.py             # Тесты обработки видео и линий
├── test_matrix.py.bak       # Резервная копия
└── __pycache__/             # Кэш Python
```

## 🧪 Тесты

### `test_matrix.py`
Тесты для работы с матрицами и A* алгоритма поиска пути.

**Что тестируется:**
- ✅ Создание матриц
- ✅ Работа A* алгоритма
- ✅ Поиск пути от стартовой позиции к целевой
- ✅ Обработка препятствий

**Запуск:**
```bash
python tests/test_matrix.py
```

**Пример теста:**
```python
import numpy as np
from br.pathfinding.a_star import algoritm_A

# Создать тестовую карту
map_matrix = np.zeros((10, 10))
map_matrix[5][5] = 1  # Препятствие

# Найти путь
result = algoritm_A(map_matrix, 10, 10, 0, 0, 9, 9, 1)

# Проверить что путь найден
assert result is not None
assert len(result) > 0
```

### `test_line.py`
Тесты для обработки видео и обнаружения линий.

**Что тестируется:**
- ✅ Захват видео с камеры
- ✅ Обработка кадров
- ✅ Обнаружение линий в изображении
- ✅ Анализ видеопотока

**Требования:**
- Веб-камера или видеофайл
- OpenCV (`cv2`)
- NumPy

**Запуск:**
```bash
python tests/test_line.py
```

**Внимание:** Этот тест требует видеоустройство или видеофайл. Может требовать настройки путей к видеофайлам.

## 🚀 Запуск всех тестов

### Вариант 1: Запустить каждый тест отдельно
```bash
cd tests
python test_matrix.py
python test_line.py
```

### Вариант 2: Используя Python unittest
```bash
python -m pytest tests/
```

### Вариант 3: Используя unittest
```bash
python -m unittest discover tests/
```

## 📋 Требования для тестов

```bash
pip install numpy opencv-python
```

## 🔧 Настройка тестов

### Тестирование с собственными данными

#### Для test_matrix.py
Отредактируйте параметры в файле:
```python
# Размер карты
GRID_SIZE = 10

# Стартовая позиция
START_X, START_Y = 0, 0

# Целевая позиция
TARGET_X, TARGET_Y = 9, 9

# Количество препятствий
OBSTACLES = [(5, 5), (5, 6), (6, 5)]
```

#### Для test_line.py
Укажите источник видео:
```python
# Веб-камера (по умолчанию 0)
cap = cv2.VideoCapture(0)

# Или видеофайл
# cap = cv2.VideoCapture('path/to/video.mp4')
```

## 📊 Примеры тестовых случаев

### Простой путь (без препятствий)
```
Карта: 10x10
Старт: (0, 0)
Цель: (9, 9)
Ожидаемый результат: Путь найден без отклонений
```

### Путь с препятствием
```
Карта: 10x10
Старт: (0, 0)
Цель: (9, 9)
Препятствие: (5, 5)
Ожидаемый результат: Путь найден с обходом препятствия
```

### Недостижимая цель
```
Карта: 10x10
Старт: (0, 0)
Цель: (9, 9)
Препятствие: полная стена от (4, 0) до (4, 9)
Ожидаемый результат: Путь не найден
```

## 🐛 Отладка

### Если test_matrix.py не работает
```bash
# Проверить что модули загружаются
python -c "from br.pathfinding.a_star import algoritm_A; print('OK')"

# Проверить что numpy установлен
python -c "import numpy; print(numpy.__version__)"
```

### Если test_line.py не работает
```bash
# Проверить что OpenCV установлен
python -c "import cv2; print(cv2.__version__)"

# Проверить доступность камеры
python -c "import cv2; cap = cv2.VideoCapture(0); print(cap.isOpened())"
```

## 📈 Результаты тестов

После запуска тестов вы должны увидеть:
- Количество пройденных тестов
- Количество ошибок
- Время выполнения

Пример успешного результата:
```
Ran 5 tests in 0.234s
OK
```

## 🔗 Связанные файлы

- [BattleBot Module](../br/README.md)
- [A* Алгоритм](../br/pathfinding/a_star.py)
- [Главный README](../README.md)

## 📚 Дополнительная информация

- [Python unittest документация](https://docs.python.org/3/library/unittest.html)
- [OpenCV документация](https://docs.opencv.org/)
- [NumPy тестирование](https://numpy.org/doc/stable/reference/testing.html)
