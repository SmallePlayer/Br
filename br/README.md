# BattleBot Module 🤖

Основной модуль для управления роботом с алгоритмами поиска пути и контроля движения.

## 📁 Структура

```
br/
├── __init__.py                  # Инициализация модуля
│
├── pathfinding/                 # Модуль поиска пути
│   ├── __init__.py
│   ├── a_star.py               # A* алгоритм поиска пути
│   ├── heuristics.py           # Эвристики (Манхэттен расстояние)
│   └── evklid.py               # Евклидово расстояние
│
├── robot/                       # Модуль управления роботом
│   ├── __init__.py
│   ├── controller.py           # Контроллер с A* алгоритмом
│   ├── bren.py                 # Алгоритм движения Брента
│   └── map_info.py             # Информация о карте и примеры
│
└── utils/                       # Вспомогательные функции
    └── __init__.py
```

## 🔍 Подмодули

### pathfinding/ - Поиск пути

#### `a_star.py`
Реализация A* алгоритма для поиска оптимального пути на сетке.

```python
from br.pathfinding.a_star import algoritm_A
import numpy as np

# Создать матрицу карты
map_matrix = np.zeros((500, 500))
map_matrix[100][100] = 1  # Препятствие

# Найти путь от (0,0) до (474, 458)
path = algoritm_A(
    map_matrix=map_matrix,
    rows=500,
    cols=500,
    x_self=0,
    y_self=0,
    x_target=474,
    y_target=458,
    step=1
)
```

**Функция:**
- `algoritm_A(map_matrix, rows, cols, x_self, y_self, x_target, y_target, step)`
  - Возвращает список координат пути

#### `heuristics.py`
Эвристические функции для A*.

```python
from br.pathfinding import heuristics

# Манхэттен расстояние (для сетки с движением 4-8)
dist = heuristics.manhed_distance((0, 0), (10, 10))
```

**Функции:**
- `manhed_distance(start, target)` - Манхэттенское расстояние
  
#### `evklid.py`
Евклидово расстояние.

```python
from br.pathfinding.evklid import evklid_distance

# Евклидово расстояние
dist = evklid_distance((0, 0), (10, 10))
```

### robot/ - Управление роботом

#### `controller.py`
Контроллер для управления движением робота с использованием A*.

```python
from br.robot.controller import algoritm_A

# Использование A* для управления роботом
# Эта функция использует A* из pathfinding
```

#### `bren.py`
Специфичный алгоритм движения - алгоритм Брента для управления движением робота.

```python
from br.robot.bren import robot_move

# Управление движением робота
robot_move(x, y, angle)
```

#### `map_info.py`
Информация о карте и примеры использования модуля.

```python
from br.robot.map_info import MapInfo

# Получить информацию о карте
map_info = MapInfo()
```

### utils/
Вспомогательные функции и утилиты для работы с модулем.

## 🚀 Примеры использования

### Полный пример: Поиск пути робота

```python
import numpy as np
from br.pathfinding.a_star import algoritm_A
from br.pathfinding import heuristics

# 1. Создать карту
map_matrix = np.zeros((500, 500))

# 2. Добавить препятствия
map_matrix[150:200, 150:200] = 1

# 3. Установить начальную и целевую позицию
start = (0, 0)
target = (474, 458)

# 4. Найти путь
path = algoritm_A(
    map_matrix=map_matrix,
    rows=500,
    cols=500,
    x_self=start[0],
    y_self=start[1],
    x_target=target[0],
    y_target=target[1],
    step=1
)

# 5. Использовать путь для управления роботом
print(f"Найден путь: {len(path)} шагов")
for x, y in path:
    print(f"Идти к ({x}, {y})")
```

## 📊 Зависимости

- **numpy** - для работы с матрицами
- **math** - стандартная математическая библиотека

## 🔧 Установка зависимостей

```bash
pip install numpy
```

## 📈 Сложность алгоритма A*

- **Временная сложность:** O(b^d), где b - ветвление, d - глубина
- **Пространственная сложность:** O(b^d)
- В практике с хорошей эвристикой гораздо быстрее

## 🎯 Особенности реализации

- ✅ A* с использованием эвристик (Манхэттен, Евклид)
- ✅ Поддержка 8-направлений движения (если нужно)
- ✅ Работа с препятствиями на сетке
- ✅ Оптимизация для больших карт (500x500)

## 🐛 Отладка

### Протестировать поиск пути

```bash
cd br
python -c "
from pathfinding.a_star import algoritm_A
import numpy as np

map_matrix = np.zeros((10, 10))
result = algoritm_A(map_matrix, 10, 10, 0, 0, 9, 9, 1)
print(f'Путь найден: {len(result)} шагов')
"
```

## 📚 Дополнительная информация

- [A* алгоритм на Wikipedia](https://en.wikipedia.org/wiki/A*_search_algorithm)
- [Эвристики поиска пути](https://theory.stanford.edu/~amitp/GameProgramming/)
- [NumPy документация](https://numpy.org/doc/)
